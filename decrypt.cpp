//#include "crypt.h"
#include <iostream>
int main(int argc, char* argv)
{
    if(argc == 3){
        
    }
    else {
        std::cout << "Invalid number of command line arguments: Please call using format ./Decrypt [in_message] [message_key]" << std::endl;
    }
}